from tests.smoke.base_test import BaseTest


class BaseOrganization(BaseTest):
    def setUp(self):
        # (if you are copying and pasting, update class title below)
        super(BaseOrganization, self).setUp()

    def tearDown(self):
        # (if you are copying and pasting, update class title below)
        super(BaseOrganization, self).tearDown()
